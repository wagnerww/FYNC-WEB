import { EnumTransformPipe } from './enum-transform.pipe';

describe('EnumTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
